services:
  web:
    build: .
    ports:
      - "5000:5000"
    # Chaves lidas do arquivo .env (raiz do projeto) — nunca versionar esse arquivo
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      GROQ_API_KEY: ${GROQ_API_KEY}
      SERPAPI_KEY: ${SERPAPI_KEY}
    restart: always
